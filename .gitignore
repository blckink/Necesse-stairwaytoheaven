
### IDE ###
.idea/**
*.iml
!.idea/modules.xml
!.idea/vcs.xml
!.idea/inspectionProfiles/Project_Default.xml

### Build ###
out/
*.class
*.log
*.jar
*.zip
hs_err_pid*

### Gradle ###
.gradle/
build/
!gradle-wrapper.jar

### Specific ###
steam_appid.txt
latest-crash.log

### Python ###
__pycache__/
*.pyc

### OS ###
.DS_Store

### Vanilla sprite dump (never committed; local QA reference only) ###
vanilla-sprites/

# Agent worktrees: separate checkouts of this repo, never content of it.
.claude/worktrees/

# The vanilla sprite dump: a local reference, never repo content. Several
# audits take --vanilla vanilla-sprites and resolve the game own textures
# through it. It was once committed as a symlink pointing at ITSELF, which
# blinded every audit that reads it.
vanilla-sprites
