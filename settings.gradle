/*/
 |  Settings gradle file.
 |  Edit the mod information and game path here.
 |  For additional libraries to be bundled, edit build.gradle.
/*/


// ===== Mod info ===== //
def modInfo = [
        modID          : "stairwaytoheaven",
        modName        : "Stairway to Heaven",
        modVersion     : "0.2.0",
        gameVersion    : "auto",
        modDescription : "Adds a third stairway that ascends above the clouds into the Skyreach: floating islands over an endless mistsea, three new sky biomes, new enemies, Aetherium gear and more.",
        author         : "blckink",
        clientside     : false,
        modDependencies         : [],
        modOptionalDependencies : [],
]


// ===== Necesse game directory ===== //
//   "auto" to attempt searching in default locations.
//   The NECESSE_GAME_DIR environment variable overrides auto-detection
//   (useful for CI/headless builds against the dedicated server).
def gamePath = "auto"


// ===== Use decompiled sources ===== //
//   Can be overridden with -PuseDecompiledSources=false
def useDecompiledSources = true



// ===== Project name ===== //
//   No need to edit, inherits mod name from mod info
rootProject.name = modInfo.modName


gradle.ext.modInfo = modInfo
gradle.ext.gamePath = gamePath
gradle.ext.useDecompiledSources = useDecompiledSources
